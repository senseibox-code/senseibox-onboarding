#!/bin/sh
set -eu

APP_PYTHON="/opt/senseibox/senseibox-onboarding/.venv/bin/python"

if [ "$(id -u)" -ne 0 ]; then
  exec sudo "$APP_PYTHON" -m senseibox_onboarding.app --wifi-only "$@"
fi

exec "$APP_PYTHON" -m senseibox_onboarding.app --wifi-only "$@"
